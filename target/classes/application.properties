# --- Use external SQL DB (example: MySQL) ---
# Uncomment and fill in values below to use your existing MySQL database
spring.datasource.url=jdbc:mysql://localhost:3306/PoStoGram?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=yuserednii3588
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

# --- Or example for PostgreSQL ---
#spring.datasource.url=jdbc:postgresql://<DB_HOST>:5432/<DB_NAME>
#spring.datasource.username=<DB_USER>
#spring.datasource.password=<DB_PASSWORD>
#spring.datasource.driver-class-name=org.postgresql.Driver
#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL10Dialect

# --- H2 in-memory (disabled) ---
# Original H2 configuration (in-memory) kept for reference ? comment out if using external DB
#spring.datasource.url=jdbc:h2:mem:postogram;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
#spring.datasource.driver-class-name=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=
#spring.h2.console.enabled=true
#spring.h2.console.path=/h2-console

# Security: CSRF disabled for local dev (configured in SecurityConfig)
